FROM debian:stretch-slim

RUN apt-get update \
  && apt-get install -y --no-install-recommends r-base r-base-dev git python3 \
  && apt-get clean
  
RUN R -e "install.packages(c('BiocManager', 'tidyverse', 'GGIR'), dependencies=TRUE, repos='http://cran.rstudio.com/')"
